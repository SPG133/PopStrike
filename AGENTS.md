# PopStrike 开发原则

以下规则适用于本仓库中的所有分析、修复和开发工作。

## 优先级

当原则之间存在取舍时，按以下顺序决策：

1. 在正确完成需求的前提下，优先最小改动。
2. 不以最小改动为理由制造明显的技术债；修改后的代码必须保持可维护、可扩展。
3. 实现前核对当前官方文档和成熟实践，选择经过验证且适合本项目规模的方案。

## 最小改动

- 只修改完成当前需求所必需的文件、类和代码。
- 能复用现有类、函数和数据结构时，不新建并行系统。
- 不执行与当前需求无关的重构、重命名、格式化或清理。
- 不擅自修改公开 API、`UPROPERTY`、`UFUNCTION`、蓝图引脚类型或资产路径；如果确实必须修改，先说明影响。
- 不创建不存在的地图、资源或虚假占位数据。缺少关键路径或名称时，明确说明。
- 保留用户的现有修改；不覆盖、回退或删除与任务无关的工作区内容。

## 可维护与可扩展

- 先理解现有数据流、网络权限、生命周期和资源依赖，再修改代码。
- 优先使用清晰的命名、单一职责和简单的数据流，避免隐式状态和重复逻辑。
- 对预期的小规模保持简单；不为尚未出现的需求预先引入大型框架、插件或抽象层。
- 当预期数据会增长时，使新增内容只需添加数据或少量独立代码，避免改动多个无关模块。
- 保持向后兼容。对蓝图、存档、网络复制和打包资源的破坏性修改要格外谨慎。
- 注释用于说明原因、约束和非显而易见的决策，不重复描述代码表面行为。

## 调研与架构选择

- 实现涉及架构、Unreal Engine API、网络、复制、资源管理、打包或热更时，必须先搜索当前官方文档和官方维护的示例。
- 优先级为：官方文档、引擎源码或官方示例、成熟一手技术资料；社区文章和论坛只作补充。
- 核对资料适用的 Unreal Engine 版本，避免直接套用过时 API 或已废弃方案。
- 将搜索结果转化为适合当前项目规模的最小实现，不照搬大型项目架构。
- 不得以“成熟架构”为理由超出用户当前需求的范围。
- 最终说明采用的方案、主要取舍和验证结果；如果使用了网络资料，提供关键来源。

## 验证与交付

- 修改前检查相关文件和当前 Git 状态。
- 修改后进行与风险成比例的编译、测试或静态检查。
- 不得把“源文件编译通过”表述为“整体构建成功”。如果链接或打包失败，要明确说明。
- 如果 Unreal Editor 正在占用模块 DLL，不擅自关闭编辑器；说明需要关闭编辑器后完成链接。
- 交付时简明列出修改文件、行为变化、验证结果和剩余风险。
